spring:
  application:
    name: geulhyang

  datasource:
    url: jdbc:mysql://127.0.0.1:3306/geulhyang
    username: root
    password: 1234
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update  # ??? ?? 'create', 'create-drop', 'validate' ? ??? ?????.
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect  # MySQL8Dialect ?? MySQL5Dialect
